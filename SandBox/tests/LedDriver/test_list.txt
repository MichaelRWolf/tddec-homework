All LEDS are off after the driver is initialized
+ LedsOffAfterCreate 

A single LED cvan be turned on.
+ TurnOnOneLed

A single LED can be turned off.
+ TurnOffOneLed

Multiple LEDS can be turned on/off.
+ TurnOnMultipleLeds
+ TurnOffAnyLed
+ AllOn
+ AllOff

LED's are write-only.  Hardware is not readable
+ AllOff

Turn on all LEDS
+ AllOn 

Turn off all LEDS
+ AllOff

Query LED state
Check boundary values 
+ QueryLedStateWithUpperAndLowerBounds


+ UpperAndLowerBounds

Check out-of-bounds values
